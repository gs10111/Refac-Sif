.pio

# Real WiFi credentials. Never commit. See include/secrets.example.h
include/secrets.h
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
.superpowers
backend/venv
backend/.venv
backend/.pytest_cache
.pytest_cache
__pycache__/
*.pyc
*.pyo

# Gateway: credenciais e estado do container. O repositório é PÚBLICO — o
# accessToken do ThingsBoard e o .env nunca entram. Ver gateway/README.md.
gateway/tb_gateway.json
gateway/.env
gateway/ca.pem
gateway/gw-config/

# Banco da configuração da frota (e os arquivos WAL/SHM que o SQLite cria ao
# lado). É estado de instalação, não de código. Ver backend/store/config_store.py
sif.db
sif.db-wal
sif.db-shm

# Capturas gravadas pelo servidor no diretorio de onde ele sobe. São dado de
# execução, não de código — uma entrou por engano no PR do trailer.
*.csv
